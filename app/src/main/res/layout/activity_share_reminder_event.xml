<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ShareReminderEventActivity">

    <include
        layout="@layout/layout_app_bar"
        android:id="@+id/share_event_toolbar">

    </include>
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/share_recycle_list"
        android:layout_width="match_parent"
        android:layout_height="520dp"
        android:layout_below="@+id/share_event_toolbar"/>
    <Button
        android:id="@+id/cancel_share"
        android:layout_width="400dp"
        android:layout_height="60dp"
        android:layout_below="@+id/share_recycle_list"
        android:layout_marginLeft="50dp"
        android:layout_marginRight="50dp"
        android:background="@drawable/input"
        android:text="Cancel"
        android:textSize="25dp"
        android:textAllCaps="false"
        android:textColor="@android:color/background_light"
        android:layout_marginTop="10dp"/>


</RelativeLayout>